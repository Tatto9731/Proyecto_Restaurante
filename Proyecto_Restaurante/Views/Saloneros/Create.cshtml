@model Proyecto_Restaurante.Models.Salonero

@{
    ViewData["Title"] = "Crear Salonero";
}

<div class="container">
    <h2 class="mb-3">Crear Salonero</h2>
    <p class="text-muted">Registra un nuevo salonero en el sistema</p>

    <div class="row">
        <div class="col-md-6">
            <form asp-action="Create" class="p-4 bg-white rounded shadow-sm">

                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group mb-3">
                    <label asp-for="Nombre" class="form-label"></label>
                    <input asp-for="Nombre" class="form-control" />
                    <span asp-validation-for="Nombre" class="text-danger"></span>
                </div>

                <div class="d-flex gap-2">
                    <input type="submit" value="Guardar" class="btn btn-primary" />
                    <a asp-action="Index" class="btn btn-secondary">Volver</a>
                </div>

            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}