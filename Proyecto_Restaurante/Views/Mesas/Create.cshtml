@model Proyecto_Restaurante.Models.Mesa

@{
    ViewData["Title"] = "Nueva Mesa";
}

<div class="card shadow">
    <div class="card-header bg-dark text-white">
        <h4 class="mb-0">➕ Crear Mesa</h4>
    </div>

    <div class="card-body">
        <form asp-action="Create">

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="NumeroMesa" class="form-label fw-semibold"></label>
                <input asp-for="NumeroMesa" class="form-control" placeholder="Ej: 1, 2, 3..." />
                <span asp-validation-for="NumeroMesa" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Capacidad" class="form-label fw-semibold"></label>
                <input asp-for="Capacidad" class="form-control" placeholder="Ej: 2, 4, 6 personas" />
                <span asp-validation-for="Capacidad" class="text-danger"></span>
            </div>

            <div class="d-flex justify-content-between">
                <a asp-action="Index" class="btn btn-secondary">← Volver</a>
                <button type="submit" class="btn btn-gold">Guardar</button>
            </div>

        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}